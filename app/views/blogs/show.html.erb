<h3> <%= @blog.title %></h3>
<p><%= @blog.description %></p>

<%= link_to 'Edit blog post', edit_blog_path(@blog) %>
<%= link_to 'Delete', blog_path(@blog), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger" %>